# Blurns

Utilities to control the blur of the elements

| Class           | Properties                               |
| --------------- | ---------------------------------------- |
| **blur-xs**     | `filter: blur(var(--dash-size-xs));`     |
| **blur-sm**     | `filter: blur(var(--dash-size-sm));`     |
| **blur-md**     | `filter: blur(var(--dash-size-md));`     |
| **blur-lg**     | `filter: blur(var(--dash-size-lg));`     |
| **blur-xl**     | `filter: blur(var(--dash-size-xl));`     |
| **blur-2xl**    | `filter: blur(var(--dash-size-2xl));`    |
| **blur-3xl**    | `filter: blur(var(--dash-size-3xl));`    |
| **blur-4xl**    | `filter: blur(var(--dash-size-4xl));`    |
| **blur-5xl**    | `filter: blur(var(--dash-size-5xl));`    |
| **blur-6xl**    | `filter: blur(var(--dash-size-6xl));`    |
| **blur-7xl**    | `filter: blur(var(--dash-size-7xl));`    |
| **blur-8xl**    | `filter: blur(var(--dash-size-8xl));`    |
| **blur-9xl**    | `filter: blur(var(--dash-size-9xl));`    |
| **blur-full**   | `filter: blur(var(--dash-size-full));`   |
| **blur-screen** | `filter: blur(var(--dash-size-screen));` |
